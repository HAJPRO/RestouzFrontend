import { defineStore } from "pinia";
// import { useToast } from "../../UI/Toast.vue";
// import { Loading } from "../../utils/Loading";

// const { toast } = useToast();
// const loading = Loading();

export const MenuStore = defineStore('MenuStore', {
  state: () => ({
    isModal: false,
   
  }),

  actions: {
  
  }
});